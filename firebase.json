{
    "firestore": [
        {
            "database": "monim1",
            "rules": "firestore.rules",
            "indexes": "firestore.indexes.json"
        },
        {
            "database": "monim-dev",
            "rules": "firestore.rules",
            "indexes": "firestore.indexes.json"
        }
    ],
    "emulators": {
        "firestore": {
            "port": 8080
        }
    }
}
